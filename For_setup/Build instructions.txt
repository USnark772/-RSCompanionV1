-   Add new folder for build version (ex. Build 1.1) in RedScientific/Companion_Builds
-   Add all needed modules to folder and rewrite all imports to point to same level
-   Edit filepaths in general_defs.py to match new locations
-   copy setup.py to build version directory
-   Edit version number in setup.py to build version
-   in cmd go to build version directory
-   command: python setup.py build
-   add inno setup script to build directory
-   inno setup compile
-   Output folder has the installer

Things to check for each build:
-   All imports
-   general_defs.py filepaths
-   setup.py version number
-   inno setup version number and filepaths
